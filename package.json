{
  "name": "ai-skills-framework",
  "version": "0.2.0",
  "private": true,
  "description": "Minic's deterministic Laravel and TypeScript agent skill framework",
  "author": "Minic",
  "repository": {
    "type": "git",
    "url": "https://github.com/emipac/skills"
  },
  "license": "MIT",
  "scripts": {
    "changeset": "changeset",
    "sync-version": "node scripts/sync-version.mjs",
    "version": "changeset version && npm run sync-version",
    "validate": "node scripts/validate-repo.mjs",
    "test:unit": "node --test tests/*.test.mjs",
    "test:install": "node scripts/smoke-install.mjs"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.30.0"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "npm@10.9.4"
}
