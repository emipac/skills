interface:
  display_name: "To Tickets"
  short_description: "Create ready tracer-bullet contracts"
  default_prompt: "Decompose the ready feature contract into vertical delivery contracts with an acyclic blocker graph."
policy:
  allow_implicit_invocation: false
