adapter: linear

# Linear adapter

Use the connected Linear integration available to the client. Resolve the
workspace, team, project, workflow states, and relation capabilities before the
first write; retain stable issue identifiers in durable artifacts.

Create issues before wiring parent and blocking relationships. The frontier is
open, unassigned work with no open blockers. Claim by assignment and move
through the team's existing states; never create substitute states merely to
match framework vocabulary.
