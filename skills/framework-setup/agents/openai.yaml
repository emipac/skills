interface:
  display_name: "Framework Setup"
  short_description: "Discover and configure the lifecycle contract"
  default_prompt: "Configure this repository, including backend/frontend profiles, confirmed source scopes, safe exact verification commands, and explicit script exclusions, without modifying AGENTS.md."
policy:
  allow_implicit_invocation: false
